
//take the example of form input name, age , city come from frontend as a object
/*
const getInput = (input) => {
     //object destructuring
     const { name, age, city } = input;
     console.log(name, age, city);
}

const obj = {
      name: 'John',
      age: 30,
      city: 'New York'
}
getInput(obj)


*/

//take a example of multiple values in object

/*
const getValues = (input) => {
 //loop though on it when we are dealing with object e use foreach loop
    input.forEach((value) => {
    const { id, name } = value;
    console.log(id, name);
    });
}

const values =[
  {
   id:"1",
   name : 'John',
  },
 {
  id:"2",
  name : 'John 1',
 },
 {
   id:"3",
   name : 'John 2',
 },
 {
    id:"4",
    name : 'John 3',
 }

];

getValues(values);

 */

//suppose there is a value like name

